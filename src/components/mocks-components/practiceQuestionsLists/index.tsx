"use client";

import { MocksItem } from "@/types";
import React from "react";

interface IPracticeQuestionList {
  questions: MocksItem[];
}

export default function PracticeQuestionList(props: IPracticeQuestionList) {
  
  return <React.Fragment>
    
  </React.Fragment>;
}
